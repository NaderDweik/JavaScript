// Problem 2 (Easy): 
// Write a React component that displays a button. 
// When the button is clicked, it should toggle between showing "ON" and "OFF".
//
// Expected behavior:
// - Initially, the button should display "OFF".
// - When clicked, it should change to "ON".
// - Clicking it again should change it back to "OFF".



const reverseString = (str) => {
    return str.split("").reverse().join("");
  };
  
  console.log(reverseString("hello")); 
  
